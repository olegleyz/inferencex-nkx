name: Template - Collect Evals

on:
  workflow_call:
    inputs:
      result-prefix:
        required: false
        type: string
        default: ''
      source-run-id:
        required: false
        type: string
        default: ''
  workflow_dispatch:
    inputs:
      source-run-id:
        description: Existing workflow run whose eval artifacts should be collected
        required: true
        type: string
      result-prefix:
        description: Optional eval artifact prefix
        required: false
        type: string
        default: ''

permissions:
  contents: read

jobs:
  collect-evals:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          token: ${{ github.token }}
          fetch-depth: 0

      - name: Download eval artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: eval_results/
          pattern: ${{ inputs.result-prefix && format('eval_{0}_*', inputs.result-prefix) || 'eval_*' }}
          github-token: ${{ github.token }}
          run-id: ${{ inputs.source-run-id || github.run_id }}

      - name: Summarize evals
        run: |
          pip install tabulate
          echo "## Eval Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          python3 utils/collect_eval_results.py eval_results/ ${{ inputs.result-prefix || 'all' }} >> $GITHUB_STEP_SUMMARY

      - name: Upload aggregated evals
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: eval_results_${{ inputs.result-prefix || 'all' }}
          path: agg_eval_${{ inputs.result-prefix || 'all' }}.json

      - name: Cleanup downloaded eval artifacts
        if: ${{ always() }}
        run: |
          rm -rf eval_results/ || true
